<% content_for(:sub_header) do %>
  <p>Here is a re-creation of Bill Mill's 
    <a href="http://billmill.org/static/canvastutorial/index.html" target="_blank">excellent 
    canvas tutorial</a> with CoffeeScript</p>
<% end %>

<dl class="contained tabs">
  <dd><a href="#setup" class="active">Setup</a></dd>
  <dd><a href="#comments">Comments</a></dd>
</dl>

<ul class="tabs-content contained">
  <li class="active" id="setupTab">
    <h4>Setup with a few files...</h4>
    <p>To start this project, we will create a new folder named <em>breakout</em> 
      and place into it three files:</p>
    <p><strong class="black">breakout.html</strong> will contain the HTML code for 
      our game. This will rarely be changed.</p>
    <p><strong class="black">breakout.js.coffee</strong> will contain the guts of 
      the game. This will be converted to raw JavaScript.</p>
    <p><strong class="black">breakout.js</strong> will contain our raw JavaScript 
      generated by <em>breakout.js.coffee</em>. This will never be modified directly.</p>
    <p>In the <em>breakout.html</em> file, let's paste the following code:</p>
    <p><script type="text/javascript" src="http://snipt.net/embed/60825c8af0e59997458d096129a36f93"></script></p>
  </li>
  <li id="commentsTab">
    <%= render :partial => "/includes/disqus" %>
  </li>
</ul>